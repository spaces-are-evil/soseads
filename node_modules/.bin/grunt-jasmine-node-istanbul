#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jasmine-node-istanbul/bin/grunt-jasmine-node-istanbul" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jasmine-node-istanbul/bin/grunt-jasmine-node-istanbul" "$@"
  ret=$?
fi
exit $ret
